# Top Buyers and Sellers Per Symbol - Sectors Financial API

Top Buyers and Sellers Per Symbol

cURL

```
curl --request GET \
  --url https://api.sectors.app/v2/broker-summary/{symbol}/top/ \
  --header 'Authorization: <api-key>'
```

```
import requests

url = "https://api.sectors.app/v2/broker-summary/{symbol}/top/"

headers = {"Authorization": "<api-key>"}

response = requests.get(url, headers=headers)

print(response.text)
```

```
const options = {method: 'GET', headers: {Authorization: '<api-key>'}};

fetch('https://api.sectors.app/v2/broker-summary/{symbol}/top/', options)
  .then(res => res.json())
  .then(res => console.log(res))
  .catch(err => console.error(err));
```

```
<?php

$curl = curl_init();

curl_setopt_array($curl, [
  CURLOPT_URL => "https://api.sectors.app/v2/broker-summary/{symbol}/top/",
  CURLOPT_RETURNTRANSFER => true,
  CURLOPT_ENCODING => "",
  CURLOPT_MAXREDIRS => 10,
  CURLOPT_TIMEOUT => 30,
  CURLOPT_HTTP_VERSION => CURL_HTTP_VERSION_1_1,
  CURLOPT_CUSTOMREQUEST => "GET",
  CURLOPT_HTTPHEADER => [
    "Authorization: <api-key>"
  ],
]);

$response = curl_exec($curl);
$err = curl_error($curl);

curl_close($curl);

if ($err) {
  echo "cURL Error #:" . $err;
} else {
  echo $response;
}
```

```
package main

import (
	"fmt"
	"net/http"
	"io"
)

func main() {

	url := "https://api.sectors.app/v2/broker-summary/{symbol}/top/"

	req, _ := http.NewRequest("GET", url, nil)

	req.Header.Add("Authorization", "<api-key>")

	res, _ := http.DefaultClient.Do(req)

	defer res.Body.Close()
	body, _ := io.ReadAll(res.Body)

	fmt.Println(string(body))

}
```

```
HttpResponse<String> response = Unirest.get("https://api.sectors.app/v2/broker-summary/{symbol}/top/")
  .header("Authorization", "<api-key>")
  .asString();
```

```
require 'uri'
require 'net/http'

url = URI("https://api.sectors.app/v2/broker-summary/{symbol}/top/")

http = Net::HTTP.new(url.host, url.port)
http.use_ssl = true

request = Net::HTTP::Get.new(url)
request["Authorization"] = '<api-key>'

response = http.request(request)
puts response.read_body
```

200

404

429

```
{
  "symbol": "BBCA.JK",
  "start": "2025-05-01",
  "end": "2025-05-14",
  "origin": "all",
  "cohort": "all",
  "top_buyers": [
    {
      "rank": 1,
      "broker_code": "KZ",
      "net_idr": 645536242500,
      "buy_idr": 1163325432500,
      "sell_idr": 517789190000
    }
  ],
  "top_sellers": [
    {
      "rank": 1,
      "broker_code": "BK",
      "net_idr": -318961117500,
      "buy_idr": 561727337500,
      "sell_idr": 880688455000
    }
  ]
}
```

```
{
  "error": "Symbol 'XYZA' not found in broker data."
}
```

```
{
  "error": "RATE_LIMIT_EXCEEDED",
  "message": "Rate limit exceeded. Consider upgrading."
}
```

Brokers

# Top Buyers and Sellers Per Symbol

Returns the brokers most actively accumulating and distributing a single IDX ticker over a date range. `top_buyers` ranks brokers by net buy value (largest positive net IDR first); `top_sellers` ranks brokers by net sell value (largest negative net IDR first). Useful for spotting institutional accumulation or distribution patterns on a specific stock.

IDX symbol: 4 letters, optionally followed by `.jk` (case-insensitive). E.g. `BBCA`, `GOTO`.

Costs 2 API credits.

GET

/

v2

/

broker-summary

/

{symbol}

/

top

/

Try it

Top Buyers and Sellers Per Symbol

cURL

```
curl --request GET \
  --url https://api.sectors.app/v2/broker-summary/{symbol}/top/ \
  --header 'Authorization: <api-key>'
```

```
import requests

url = "https://api.sectors.app/v2/broker-summary/{symbol}/top/"

headers = {"Authorization": "<api-key>"}

response = requests.get(url, headers=headers)

print(response.text)
```

```
const options = {method: 'GET', headers: {Authorization: '<api-key>'}};

fetch('https://api.sectors.app/v2/broker-summary/{symbol}/top/', options)
  .then(res => res.json())
  .then(res => console.log(res))
  .catch(err => console.error(err));
```

```
<?php

$curl = curl_init();

curl_setopt_array($curl, [
  CURLOPT_URL => "https://api.sectors.app/v2/broker-summary/{symbol}/top/",
  CURLOPT_RETURNTRANSFER => true,
  CURLOPT_ENCODING => "",
  CURLOPT_MAXREDIRS => 10,
  CURLOPT_TIMEOUT => 30,
  CURLOPT_HTTP_VERSION => CURL_HTTP_VERSION_1_1,
  CURLOPT_CUSTOMREQUEST => "GET",
  CURLOPT_HTTPHEADER => [
    "Authorization: <api-key>"
  ],
]);

$response = curl_exec($curl);
$err = curl_error($curl);

curl_close($curl);

if ($err) {
  echo "cURL Error #:" . $err;
} else {
  echo $response;
}
```

```
package main

import (
	"fmt"
	"net/http"
	"io"
)

func main() {

	url := "https://api.sectors.app/v2/broker-summary/{symbol}/top/"

	req, _ := http.NewRequest("GET", url, nil)

	req.Header.Add("Authorization", "<api-key>")

	res, _ := http.DefaultClient.Do(req)

	defer res.Body.Close()
	body, _ := io.ReadAll(res.Body)

	fmt.Println(string(body))

}
```

```
HttpResponse<String> response = Unirest.get("https://api.sectors.app/v2/broker-summary/{symbol}/top/")
  .header("Authorization", "<api-key>")
  .asString();
```

```
require 'uri'
require 'net/http'

url = URI("https://api.sectors.app/v2/broker-summary/{symbol}/top/")

http = Net::HTTP.new(url.host, url.port)
http.use_ssl = true

request = Net::HTTP::Get.new(url)
request["Authorization"] = '<api-key>'

response = http.request(request)
puts response.read_body
```

200

404

429

```
{
  "symbol": "BBCA.JK",
  "start": "2025-05-01",
  "end": "2025-05-14",
  "origin": "all",
  "cohort": "all",
  "top_buyers": [
    {
      "rank": 1,
      "broker_code": "KZ",
      "net_idr": 645536242500,
      "buy_idr": 1163325432500,
      "sell_idr": 517789190000
    }
  ],
  "top_sellers": [
    {
      "rank": 1,
      "broker_code": "BK",
      "net_idr": -318961117500,
      "buy_idr": 561727337500,
      "sell_idr": 880688455000
    }
  ]
}
```

```
{
  "error": "Symbol 'XYZA' not found in broker data."
}
```

```
{
  "error": "RATE_LIMIT_EXCEEDED",
  "message": "Rate limit exceeded. Consider upgrading."
}
```

#### Authorizations

[​

](#authorization-authorization)

Authorization

string

header

required

Global API Key Authorization

#### Path Parameters

[​

](#parameter-symbol)

symbol

string

required

IDX ticker symbol. E.g. `BBCA`, `GOTO`.

#### Query Parameters

[​

](#parameter-start)

start

string

Start date (YYYY-MM-DD). Default: end - 30 days.

[​

](#parameter-end)

end

string

End date (YYYY-MM-DD). Default: today.

[​

](#parameter-cohort)

cohort

enum<string>

Filter brokers by cohort (case-insensitive). Default `all`.

Available options:

`all`,

`institutional`,

`mixed`,

`retail`,

`unknown`

[​

](#parameter-n-brokers)

n\_brokers

integer

How many buyers and sellers to return each (default 10, max 90).

Required range: `1 <= x <= 90`

[​

](#parameter-origin)

origin

enum<string>

Filter brokers by origin. Default `all`.

Available options:

`all`,

`domestic`,

`foreign`

#### Response

200

application/json

Top net buyers and top net sellers for the symbol over the date range.

[​

](#response-symbol)

symbol

string

required

Ticker symbol.

[​

](#response-start)

start

string<date>

required

Range start date.

[​

](#response-end)

end

string<date>

required

Range end date.

[​

](#response-origin)

origin

string

required

[​

](#response-cohort)

cohort

string

required

[​

](#response-top-buyers)

top\_buyers

object\[\]

required

Show child attributes

[​

](#response-top-sellers)

top\_sellers

object\[\]

required

Show child attributes