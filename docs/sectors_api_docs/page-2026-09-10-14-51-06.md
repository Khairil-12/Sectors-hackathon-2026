# Daily Net Foreign Inflow - Sectors Financial API

Daily Net Foreign Inflow

cURL

```
curl --request GET \
  --url https://api.sectors.app/v2/foreign-flow/{symbol}/ \
  --header 'Authorization: <api-key>'
```

```
import requests

url = "https://api.sectors.app/v2/foreign-flow/{symbol}/"

headers = {"Authorization": "<api-key>"}

response = requests.get(url, headers=headers)

print(response.text)
```

```
const options = {method: 'GET', headers: {Authorization: '<api-key>'}};

fetch('https://api.sectors.app/v2/foreign-flow/{symbol}/', options)
  .then(res => res.json())
  .then(res => console.log(res))
  .catch(err => console.error(err));
```

```
<?php

$curl = curl_init();

curl_setopt_array($curl, [
  CURLOPT_URL => "https://api.sectors.app/v2/foreign-flow/{symbol}/",
  CURLOPT_RETURNTRANSFER => true,
  CURLOPT_ENCODING => "",
  CURLOPT_MAXREDIRS => 10,
  CURLOPT_TIMEOUT => 30,
  CURLOPT_HTTP_VERSION => CURL_HTTP_VERSION_1_1,
  CURLOPT_CUSTOMREQUEST => "GET",
  CURLOPT_HTTPHEADER => [
    "Authorization: <api-key>"
  ],
]);

$response = curl_exec($curl);
$err = curl_error($curl);

curl_close($curl);

if ($err) {
  echo "cURL Error #:" . $err;
} else {
  echo $response;
}
```

```
package main

import (
	"fmt"
	"net/http"
	"io"
)

func main() {

	url := "https://api.sectors.app/v2/foreign-flow/{symbol}/"

	req, _ := http.NewRequest("GET", url, nil)

	req.Header.Add("Authorization", "<api-key>")

	res, _ := http.DefaultClient.Do(req)

	defer res.Body.Close()
	body, _ := io.ReadAll(res.Body)

	fmt.Println(string(body))

}
```

```
HttpResponse<String> response = Unirest.get("https://api.sectors.app/v2/foreign-flow/{symbol}/")
  .header("Authorization", "<api-key>")
  .asString();
```

```
require 'uri'
require 'net/http'

url = URI("https://api.sectors.app/v2/foreign-flow/{symbol}/")

http = Net::HTTP.new(url.host, url.port)
http.use_ssl = true

request = Net::HTTP::Get.new(url)
request["Authorization"] = '<api-key>'

response = http.request(request)
puts response.read_body
```

200

404

429

```
{
  "symbol": "BBCA.JK",
  "start": "2025-05-01",
  "end": "2025-05-05",
  "data": [
    {
      "date": "2025-05-02",
      "net_foreign_inflow": 199859810000
    }
  ]
}
```

```
{
  "error": "Symbol 'XYZA' not found in broker data."
}
```

```
{
  "error": "RATE_LIMIT_EXCEEDED",
  "message": "Rate limit exceeded. Consider upgrading."
}
```

Brokers

# Daily Net Foreign Inflow

Daily net foreign-broker inflow (IDR) for one IDX ticker over a date range up to 90 days. Positive `net_foreign_inflow` means foreign brokers were net buyers that day; negative means foreign brokers were net sellers. Useful for tracking foreign sentiment and capital flow on a specific stock.

IDX symbol: 4 letters, optionally followed by `.jk` (case-insensitive). E.g. `BBCA`, `GOTO`.

Only foreign flow is returned because the exchange is a closed market: for any `(symbol, date)`, foreign and domestic net values always sum to zero, so domestic flow is simply `-net_foreign_inflow`.

Costs 1 API credit.

GET

/

v2

/

foreign-flow

/

{symbol}

/

Try it

Daily Net Foreign Inflow

cURL

```
curl --request GET \
  --url https://api.sectors.app/v2/foreign-flow/{symbol}/ \
  --header 'Authorization: <api-key>'
```

```
import requests

url = "https://api.sectors.app/v2/foreign-flow/{symbol}/"

headers = {"Authorization": "<api-key>"}

response = requests.get(url, headers=headers)

print(response.text)
```

```
const options = {method: 'GET', headers: {Authorization: '<api-key>'}};

fetch('https://api.sectors.app/v2/foreign-flow/{symbol}/', options)
  .then(res => res.json())
  .then(res => console.log(res))
  .catch(err => console.error(err));
```

```
<?php

$curl = curl_init();

curl_setopt_array($curl, [
  CURLOPT_URL => "https://api.sectors.app/v2/foreign-flow/{symbol}/",
  CURLOPT_RETURNTRANSFER => true,
  CURLOPT_ENCODING => "",
  CURLOPT_MAXREDIRS => 10,
  CURLOPT_TIMEOUT => 30,
  CURLOPT_HTTP_VERSION => CURL_HTTP_VERSION_1_1,
  CURLOPT_CUSTOMREQUEST => "GET",
  CURLOPT_HTTPHEADER => [
    "Authorization: <api-key>"
  ],
]);

$response = curl_exec($curl);
$err = curl_error($curl);

curl_close($curl);

if ($err) {
  echo "cURL Error #:" . $err;
} else {
  echo $response;
}
```

```
package main

import (
	"fmt"
	"net/http"
	"io"
)

func main() {

	url := "https://api.sectors.app/v2/foreign-flow/{symbol}/"

	req, _ := http.NewRequest("GET", url, nil)

	req.Header.Add("Authorization", "<api-key>")

	res, _ := http.DefaultClient.Do(req)

	defer res.Body.Close()
	body, _ := io.ReadAll(res.Body)

	fmt.Println(string(body))

}
```

```
HttpResponse<String> response = Unirest.get("https://api.sectors.app/v2/foreign-flow/{symbol}/")
  .header("Authorization", "<api-key>")
  .asString();
```

```
require 'uri'
require 'net/http'

url = URI("https://api.sectors.app/v2/foreign-flow/{symbol}/")

http = Net::HTTP.new(url.host, url.port)
http.use_ssl = true

request = Net::HTTP::Get.new(url)
request["Authorization"] = '<api-key>'

response = http.request(request)
puts response.read_body
```

200

404

429

```
{
  "symbol": "BBCA.JK",
  "start": "2025-05-01",
  "end": "2025-05-05",
  "data": [
    {
      "date": "2025-05-02",
      "net_foreign_inflow": 199859810000
    }
  ]
}
```

```
{
  "error": "Symbol 'XYZA' not found in broker data."
}
```

```
{
  "error": "RATE_LIMIT_EXCEEDED",
  "message": "Rate limit exceeded. Consider upgrading."
}
```

#### Authorizations

[​

](#authorization-authorization)

Authorization

string

header

required

Global API Key Authorization

#### Path Parameters

[​

](#parameter-symbol)

symbol

string

required

IDX ticker symbol. E.g. `BBCA`, `GOTO`.

#### Query Parameters

[​

](#parameter-start)

start

string

Start date (YYYY-MM-DD). Default: end - 30 days.

[​

](#parameter-end)

end

string

End date (YYYY-MM-DD). Default: today.

#### Response

200

application/json

Daily net foreign inflow for the symbol.

[​

](#response-symbol)

symbol

string

required

Ticker symbol.

[​

](#response-start)

start

string<date>

required

Range start date.

[​

](#response-end)

end

string<date>

required

Range end date.

[​

](#response-data)

data

object\[\]

required

Show child attributes